fullnameOverride: postgres

global:
  security:
    allowInsecureImages: true

image:
  repository: bitnamilegacy/postgresql

auth:
  username: app
  password: localdev
  database: orders

primary:
  persistence:
    enabled: false
  service:
    ports:
      postgresql: 5432

resources: {}
